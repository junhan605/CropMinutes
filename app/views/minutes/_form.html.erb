<%= bootstrap_form_for(minute, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-10") do |f| %>
  <% if minute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(minute.errors.count, "error") %> prohibited this minute from being saved:</h2>

      <ul>
      <% minute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.text_field :company_type, label: "Type" %>
  </div>

  <div class="field">
    <%= f.text_field :location %>
  </div>

  <div class="field col-sm-offset-1" >
    <%= f.label :present %>
  </div>

  <div class="field col-sm-offset-2">
     <%= f.check_box :secretary%>
  </div>

  <div class="field col-sm-offset-2">
    <%= f.check_box :president %>
  </div>

  <div class="field col-sm-offset-2">
    <%= f.check_box :director %>
  </div>
  <div class="field col-sm-offset-2">
    <%= f.check_box :treasurer %>
  </div>

  <div class="field">
    <%= f.text_field :other_party %>
  </div>

  <div class="field">
    <%= f.text_field :escriibe_property %>
  </div>

  <div class="actions col-sm-offset-1" align="left">
    <%= f.submit class: "btn btn-primary", value: "CREATE MINUTES >>" %>
  </div>
<% end %>
